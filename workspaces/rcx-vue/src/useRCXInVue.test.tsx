import '@testing-library/jest-dom';

describe('useRCXInVue', () => {
  it('provides a way to share state between a Vue (with JSX) app and an RCX app', async () => {
    // @FIXME: add tests for rendering within vue app
  });

  it('provides a way to share state between a Vue (with templates) app and an RCX app', async () => {
    // @FIXME: add tests for rendering within vue app
  });
});
